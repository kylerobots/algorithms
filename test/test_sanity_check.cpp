#include "gtest/gtest.h"

/**
 * @brief A test that is guaranteed to pass to verify things are
 * working correctly.
 * 
 * Think of this as the "Hello world!" of tests.
 * */
TEST(SimpleTest, SimpleTestCase) {
	EXPECT_EQ(1, 1);
}